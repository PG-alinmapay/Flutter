name: flutter_payment_plugin
description: This Flutter plugin provide merchants to easy and hasslefree integration with Payment gateway API's.
version: 3.0.0
homepage:

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  convert: ^3.1.2
  crypto: ^3.0.6
  connectivity_plus: ^6.1.0

  device_info_plus: ^12.3.0
  dart_ipify: ^1.1.1
  path_provider: ^2.1.5
  apple_pay_flutter: ^1.0.4
  package_info_plus: ^8.1.0
  webview_flutter: ^4.10.0
  webview_flutter_web: ^0.2.3+4
  url_launcher: ^6.1.14
  yaml: ^3.1.1
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:

flutter:

